## this task runs on aws ubuntu ec2 instance

run terrafom file (ec2-minikube-compose) to start an instance

1- copy files to instance
2- make executable to run-script.sh (chmod 777 run-script.sh)
3- and run the script (./run-script)
4- http//instance_ip_adress:8080  